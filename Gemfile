# typed: strong

source 'https://rubygems.org'

# Specify your gem's dependencies in algolia.gemspec
gemspec

gem 'minitest-ci'

group :development do
  gem 'git-precommit'
  gem 'steep'
  gem 'yard'
end

group :test do
  gem 'simplecov'
end
